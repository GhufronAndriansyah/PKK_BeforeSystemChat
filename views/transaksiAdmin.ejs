<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Pesan Tempat Titip</title>
</head>
<h4>
    <h2>Data Transaksi</h2>
    <button><a href="transkasi/daftar">Tambah Transkasi</a></button>
    <br><br>
        <table border="1" >
                <thead>
                    <tr>
                        <th>No</th>
                        <th>Id User</th>
                        <th>Id Tempat</th>
                        <th>Nama Hewan</th>
                        <th>Waktu</th>
                        <th>Total Harga</th>
                        <th>Kode</th>
                        <th>Status</th>
                        <th>Aksi</th>
                    </tr>
                </thead>
                <tbody>
                    
                    <% if(data.length){ var no = 0 ; for(var i = 0 ; i < data.length ; i++){no++;%> 
                        <tr>
                            <td><%= no %></td>
                            <td><%= data[i].id_user %></td>
                            <td><%= data[i].id_tempat %></td>
                            <td>
                            <%if(data[i].titip.length){for(var k = 0 ; k < data[i].titip.length ; k++){;%> 
                            <%= data[i].titip[k].name_pet%><br>
                            <% }}%> 
                            </td>
                            <td><%= data[i].waktu %></td>
                            <td><%= data[i].total_harga %></td>
                            <td><%= data[i].kode %></td>
                            <td align="center"><%= data[i].status %></td>
                            <td>
                                <a href="transaksi/update/<%= data[i].id_transaksi%>" class="btn btn-secondary btn-sm">Update</a>
                                <a href="transaksi/delete/<%= data[i].id_transaksi%>" class="btn btn-secondary btn-sm">Delete</a>
                            </td>
                        </tr>
                    <% }} %> 
                </tbody>
            </table>
</h4>
<body>
</body>
</html>